from setuptools import setup, find_packages

setup(
    name='selenium_func',
    version='0.0.1',
    packages=find_packages(),
    url='https://github.com/bu-bu-BUTASAN/selenium_func',
    install_requires=['selenium'],
    )